let s1="Pragya"
let s2=" Chaudhary"
console.log(s1+s2)//strings will add
console.log(1+2)// add the numbers
console.log(1+"2")// 12 as 2 is takem as string
console.log("1"+2)//12 as 1 taken as string
console.log("1"+"2")//12 as both 1 & 2 are taken as string
console.log(1+2+"3")//sum 1 and 2 = 3 and 3rd number taken as string so 1+2=3+string 3 so 33
console.log("1"+"2"+3+4)//1 & 2 are taken as string and also 3 & 4 both are also as string as the staring operation is string so whole will be treated as string
/*-----Rule------
1.All will be treat as the first opeation means if starting is number then add them as number but string will be append if given
2. If operation start as string then whole will be applied as string not numbers*/
